apiVersion: v1
kind: Service
metadata:
  name: sovereign-alpha-api
  namespace: sovereign-alpha
spec:
  selector:
    app: sovereign-alpha-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
